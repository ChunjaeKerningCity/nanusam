<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.fullstack7.nanusam.mapper.ChatMapper">
    <insert id="regist" parameterType="net.fullstack7.nanusam.domain.ChatVO">
        INSERT INTO tbl_chat(goodsIdx, sender, receiver, content)
        VALUES(#{goodsIdx},#{sender},#{receiver},#{content})
    </insert>
    <select id="getList" resultType="net.fullstack7.nanusam.domain.ChatVO">
        SELECT idx, goodsIdx, sender, receiver, senderDel, receiverDel, content, regDate, readCheck
        FROM tbl_chat
        WHERE sender = #{memberId} OR receiver = #{memberId}
    </select>
</mapper>